import { Button } from "@fluentui/react-components";
import React from "react";

interface IProps {
   
};

const NotFound: React.FunctionComponent<IProps> = ({}): React.ReactElement => {

    return (
        <>
            <h1>Not Found</h1>
        </>
    );
};

export default NotFound;